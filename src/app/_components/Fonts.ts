import { Corinthia, Fraunces } from "next/font/google";

export const corinthia = Corinthia({
  weight: ["400"],
  subsets: ["latin"]
});

export const fraunces = Fraunces({
  weight: ["400"],
  subsets: ["latin"]
});